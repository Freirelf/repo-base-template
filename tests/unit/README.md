# tests/unit

Testes de unidades isoladas — geralmente `domain/` e `application/` — sem dependências
externas reais (banco, rede, filesystem). Use dublês (fakes/mocks) para as portas do
domínio quando necessário.
