# tests/e2e

Testes de ponta a ponta: exercitam o sistema como um usuário real faria, através da
`interface/` (ex: chamadas HTTP reais contra a aplicação rodando), sem conhecer detalhes
internos de implementação.
