# tests/integration

Testes que verificam a integração entre componentes internos reais — por exemplo, um caso
de uso de `application/` rodando contra um adapter real de `infrastructure/` (banco de
dados de teste, fila local, etc.).
